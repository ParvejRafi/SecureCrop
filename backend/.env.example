# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database URL (Render provides this automatically in production)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Database Settings (for local development only)
DB_NAME=securecrop_db
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# OpenWeather API
OPENWEATHER_API_KEY=your-openweather-api-key

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-app-password
DEFAULT_FROM_EMAIL=noreply@securecrop.com
ADMIN_EMAIL=admin@securecrop.com

# Infobip WhatsApp Configuration
INFOBIP_API_KEY=your-infobip-api-key
INFOBIP_BASE_URL=https://api.infobip.com
INFOBIP_SENDER=your-whatsapp-sender-number

# CORS Settings (for production - comma-separated list)
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com
